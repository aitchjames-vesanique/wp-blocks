/**
 * Save function - returns null because we're using render.php for server-side rendering
 * This is the recommended approach for blocks using the Interactivity API
 */
export default function save() {
    return null;
}
